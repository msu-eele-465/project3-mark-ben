#include <msp430.h>
#include <stdbool.h>


// This function will update the current status of the led
void updateled(int current_status) {
    switch (current_status) {
        case 0:
        
        case 1:

        case 2:
    }
}